TEMPLATE STATUS: PROVIDED

> 공식 템플릿: `WP_Templates/Engineering/SoftwareDetailedDesignAndUnitConstruction/TPL-SWE3-001_SW 상세설계서 템플릿.docx`
>
> ⚠️ 저작권/이용 제한: 본 템플릿은 "SW 품질교육을 위한 교육용 샘플"이며 저작권은 Synetics에 있습니다. 교육 과정 안에서 열람·복제·실습 사용은 허용되지만, 과정 밖 배포·공개·상업적 이용은 Synetics의 사전 서면 승인이 필요합니다. 이 문서를 실제 상용 프로젝트나 공개 저장소에 사용하는 것이 이 범위를 벗어나는지 사용자에게 확인하세요.

## 사용 방법

1. 원본 템플릿 파일을 직접 수정하지 않습니다. `WP_Templates/Engineering/README.md`와 `WP_Templates/PRC-TPL-001_표준 산출물 양식 등록부.xlsx` 규칙에 따라 템플릿을 복사한 뒤 파일명을 `<산출물 ID>_<산출물명>.docx`로 바꾸고, 프로젝트/버전/베이스라인 정보를 채웁니다.
2. 각 절 제목 아래의 "작성 안내" 문구는 실제 프로젝트 내용으로 **교체**합니다(안내 문구를 지우지 않고 남겨두지 않습니다).
3. 문서 편집은 `anthropic-skills:docx` 스킬 절차(기존 파일이므로 "Edit" 경로: unzip → `word/document.xml` 편집 → zip, 또는 이 환경에 pandoc/LibreOffice가 없으면 unzip 후 `<w:t>` 텍스트 노드를 직접 치환)를 따릅니다.
4. 편집 후 가능하면 PDF로 변환해 렌더링을 확인합니다(이 환경에 LibreOffice가 없으면 XML 상에서 텍스트가 올바른 위치에 들어갔는지 재확인).

## 문서 구조 (15개 절) 및 이 스킬의 대응 절차

| 절 | 제목 | 채울 내용 요약 | 대응 스킬 절차 |
|---|---|---|---|
| 1 | 목적 및 적용범위 | 목적, 적용 제품/컴포넌트/생명주기 단계, 적용 경계(포함/제외) | §1 입력 파악 |
| 2 | 모듈 분해 | 구현 단위 ID/책임/소스 위치 | §2 |
| 3 | 상세 호출관계 | 함수/클래스 호출 순서, 의존 방향, 데이터 흐름 (다이어그램) | §3 |
| 4 | 공통 자료형 | 공통 데이터 구조, 열거형, 단위, 범위, 불변조건, 직렬화 규칙 | §4 |
| 5 | 핵심 함수 계약 | 함수별 입력/출력/사전·사후조건/부작용/예외/시간제약 | §5 |
| 6 | 핵심 알고리즘 | 처리 순서, 판단 조건, 경계값, 계산 근거 (의사코드/흐름도) | §6 |
| 7 | 정책 의사결정표 | 입력 조건 조합, 우선순위, 기대 동작, 충돌 해결 규칙 | §7 |
| 8 | 상태전이 상세 | 상태 저장 위치, 전이 함수, 이벤트, 가드, 타이머, 초기화 | §8 |
| 9 | Web 및 API 상세 | 엔드포인트, 요청/응답 구조, 검증, 오류 코드, 세션/보안 경계 | §9 |
| 10 | 오류와 방어 동작 | 유효하지 않은 입력/예외/자원 실패의 검출·처리·기록·복구 | §10 |
| 11 | 코딩 및 검증 규칙 | 코딩 표준, 정적분석, 단위검증, 커버리지, 리뷰 기준 | §11 |
| 12 | 단위와 요구사항 할당 | 구현 단위 ↔ 아키텍처 요소 ↔ SW 요구사항 ↔ 단위시험 연결 | §13 |
| 13 | 구현 경계 | 생성 코드, 외부 라이브러리, 플랫폼 종속부, 구현하지 않는 범위 | §13 |
| 14 | 추적성 | 아키텍처/상세설계/소스/함수/단위시험 양방향 연결 | §14 |
| 15 | 참고자료 | 아키텍처 설계서, 코딩규칙, API 문서, 외부 라이브러리 자료 | §15 |

문서 공통 헤더(문서 통제, 변경 이력, 작성/검토/승인 상태)는 템플릿에 이미 표로 존재하므로 그 표를 그대로 채웁니다.
